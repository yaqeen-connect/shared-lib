export enum ACCOUNT_TYPE {
  ADMIN = "admin",
  MODERATOR = "moderator",
  USER = "user",
}

export enum GENDER {
  MALE = "male",
  FEMALE = "female",
  OTHER = "other",
}

export enum ACCOUNT_STATUS {
  ACTIVE = "active",
  INACTIVE = "inactive",
  BANNED = "banned",
}

export enum SUBSCRIPTION_PLAN {
  FREE = "free",
  PREMIUM = "premium",
}
